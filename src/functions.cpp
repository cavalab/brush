#include "operators.h"


// vector<Operator*> Operators = { 
//                                new Add<float(float,float)>(),
//                                new Sub<float(float,float)>()
//                               };
// // auto op = Add<float(float,float)>();

// for (const auto& op : Operators)
// {
//     auto node = WeightedDxNode(op.name, op, op.df);
// }