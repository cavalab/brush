name: brush
channels:
    - conda-forge
dependencies:
    - python>=3.9 #=3.8.2
    - cmake #=3.18.*
    - eigen #=3.4.*
    - fmt
    - gcc >= 12.0
    - gxx >= 12.0
    - ninja
    - ceres-solver=2.1.0
    - pybind11>=2.6.2
    - taskflow
    - boost-cpp
    - pytest #=6.2.4
    - pydot
    - scikit-learn
    - pandas
    # not required for install the c++ library (but used in the wrapper)
    - jupyter
    - ipython
    - pip
    - nlohmann_json
    - pybind11_json
    # Building documentation
    - doxygen
    - sphinx
    - pip:
      - graphviz
      - deap
      - pmlb